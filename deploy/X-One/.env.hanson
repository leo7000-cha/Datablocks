# ==============================================================================
# DLM 환경변수 — 한국손해보험 (PROD: OS 설치형 MariaDB)
# ==============================================================================
# ★ MariaDB 가 호스트 OS 에 직접 설치됨 (Docker 컨테이너 아님)
# ★ 컨테이너 → 호스트 DB 접속은 host.docker.internal (host-gateway) 사용

# --- ★ 호스트 MariaDB 접속정보 ---
# 운영자 요구(박기용): JDBC_* 명명 사용. docker-compose.hanson.yml 이 SPRING_DATASOURCE_* 로 alias 주입.
JDBC_URL=jdbc:mariadb://host.docker.internal:3306/cotdl?serverTimezone=UTC&autoReconnect=true&allowMultiQueries=true
JDBC_USERNAME=cotdl
JDBC_PASSWORD=!Dlm1234
SPRING_PROFILES_ACTIVE=local

# --- ★ MariaDB 호스트 (배포 스크립트가 연결 테스트 용으로 사용) ---
# 기본값 host.docker.internal (권장) — 필요 시 호스트 실제 IP 로 변경 가능
MARIADB_HOST=host.docker.internal
MARIADB_PORT=3306

# --- ★ 서비스 포트 (기존 서비스와 충돌 시 변경) ---
DLM_PORT=8082
AI_PORT=8000

# --- ★ Privacy-AI DB 접속정보 (OS MariaDB) ---
PRIVACY_AI_DB_HOST=host.docker.internal
PRIVACY_AI_DB_PORT=3306
PRIVACY_AI_DB_NAME=cotdl
PRIVACY_AI_DB_USER=cotdl
PRIVACY_AI_DB_PASSWORD=!Dlm1234

# --- Privacy-AI 내부 설정 ---
# Privacy-AI 는 compose 기본 네트워크로 DLM 컨테이너에 직접 접근
PRIVACY_AI_DLM_API_URL=http://dlm:8080
PRIVACY_AI_DEBUG=false

# --- Mail (선택) ---
MAIL_HOST=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=

# --- LLM 설정 (선택) ---
PRIVACY_AI_LLM_ENABLED=false
PRIVACY_AI_LLM_API_URL=
PRIVACY_AI_LLM_API_KEY=
PRIVACY_AI_LLM_MODEL=

# ==============================================================================
# X-Audit Storage Routing (감사로그 저장 DB)
# ==============================================================================
# TBL_PIIDATABASE.db=XAUDIT_DB 엔트리가 물리적 저장소를 결정.
# 최초 기동 시 Primary DS 정보로 자동 등록 → 기본은 DLM 내부 DB.
# 고객사 별도 DB 로 전환: UI 에서 XAUDIT_DB 엔트리만 수정 후 재기동.
XAUDIT_STORAGE_POOL_SIZE=4
XAUDIT_STORAGE_RETRY_INIT_SECONDS=60
